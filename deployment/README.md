# Various deployment strategies of the app

- [x] [Docker](../docker/)
- [x] [Kubernetes: Kustomizations](./kustomizations/)

`wren-engine` and `wren-engine-ibis` are now expected to run as Docker images
built from the vendored `./wren-engine` source tree in this repository. Process
mode is no longer the primary deployment path for the engine in this repo.
