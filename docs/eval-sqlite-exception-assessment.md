# Historical benchmark migration note

This note is kept only as historical context.

Current repo state:

- Spider/BIRD benchmark execution is PostgreSQL-only.
- Runtime benchmark loading from local file-based databases has been removed.
- The active docs under `wren-ai-service/eval/README.md` and
  `wren-ai-service/docs/configuration.md` are the current source of truth.

If this note and the active eval docs disagree, follow the active eval docs.
