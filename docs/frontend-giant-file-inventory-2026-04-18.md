# Frontend Giant File Inventory — 2026-04-18

对应 backlog：`docs/frontend-architecture-backlog-2026-04-18.md` Wave 0 / Wave 3。

## Top hotspots

| File                                                                        | Approx lines | Current role                                            | Recommended split direction                                                                  |
| --------------------------------------------------------------------------- | -----------: | ------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `wren-ui/src/features/knowledgePage/useKnowledgeWorkbenchPageController.ts` |          ~65 | knowledge workbench page-level hook composition         | controller-data / page-interaction builder 调用已继续下沉到独立组合 hook；当前主要保留 top-level page composition |
| `wren-ui/src/features/knowledgePage/knowledgeWorkbenchControllerViewTypes.ts` |          ~14 | knowledge workbench controller-view aggregate arg types | 已收口为 aggregate barrel；controller-view operation/state shared types 已拆到独立 lane files |
| `wren-ui/src/features/knowledgePage/knowledgeWorkbenchPageInteractionInputTypes.ts` |          ~15 | knowledge workbench page-interaction aggregate types    | 已收口为 aggregate barrel；page-interaction local/controller/data shared types 已拆到独立 lane files |
| `wren-ui/src/features/knowledgePage/useKnowledgeWorkbenchViewState.ts` |          ~26 | knowledge workbench view-state composition              | 已把 presentation/sync-effects 输入组装继续下沉到纯 builder；当前仅保留轻量组合 |
| `wren-ui/src/features/knowledgePage/buildKnowledgeWorkbenchControllerMainStageInput.ts` |          ~29 | knowledge workbench controller main-stage lane builder  | overview/detail 与 rule-sql/modeling props 已继续拆到独立 builder lane；当前仅保留轻量 merge layer |
| `wren-ui/src/features/knowledgePage/sections/knowledgeMainStageEditorSectionPropTypes.ts` |          ~79 | knowledge main-stage editor prop-builder types          | 当前承接 SQL/instruction stage builder 的共享 args/editor-state types；若继续膨胀，可按 sql 与 instruction 两组再拆 |
| `wren-ui/src/features/knowledgePage/sections/buildKnowledgeMainStageEditorsInput.ts` |          ~67 | knowledge main-stage editors-hook input builder         | 当前承接 main-stage → editors hook 的纯输入映射；如果后续需要，可再与 section builders 统一为 stage view-model lane |
| `wren-ui/src/features/knowledgePage/useKnowledgeWorkbenchControllerDataState.ts` |          ~35 | knowledge workbench controller data-state composition   | knowledge/content/modeling 输入组装已下沉到纯 builder；当前仅保留轻量组合 |
| `wren-ui/src/features/knowledgePage/knowledgeWorkbenchControllerStageTypes.ts` |          ~20 | knowledge workbench controller stage aggregate types    | 已收口为 barrel + aggregate args；lane-specific shared types 已拆到独立 stage type files |
| `wren-ui/src/features/home/thread/useThreadRecoveryOrchestration.ts`        |          ~77 | thread recovery orchestration entry hook          | refs / execution controls / effect scheduling 已继续拆到独立 hook lane；剩余主要是 top-level composition + result assembly |
| `wren-ui/src/features/knowledgePage/sections/KnowledgeWorkbenchEditorItemCard.tsx` |          ~63 | shared knowledge workbench item-card shell        | meta/actions 已继续拆到独立 lane files；剩余主要是 shared card shell + props glue |
| `wren-ui/src/features/knowledgePage/sections/KnowledgeInstructionDrawer.tsx` |          ~69 | knowledge workbench analysis-rules drawer lane    | drawer shell + asset-context panel 已继续收敛到 shared asset-editor drawer；剩余主要是 domain wrapper props glue |

## 拆分优先级

### P1：继续拆

- `features/home/thread/useThreadRecoveryOrchestration.ts`
- `features/knowledgePage/sections/KnowledgeWorkbenchEditorItemCard.tsx`
- `features/knowledgePage/sections/KnowledgeInstructionDrawer.tsx`

## 当前判断

- Wave 3 已有实质进展（knowledge feature 化、modeling workspace 抽离等）。
- `DolaAppShell.tsx` 已进一步收敛到约 199 行，不再属于 giant-file 热点。
- 已按“500 行以内即可”的当前标准继续收口：
  - `src/common.ts` 当前约 494 行
  - `src/components/learning/guide/stories.tsx` 当前约 30 行
  - `src/utils/errorHandler.tsx` 当前约 320 行
  - `src/utils/referenceDemoKnowledge.ts` 当前约 165 行
  - `src/features/knowledgePage/lists.tsx` 当前约 459 行
  - `src/features/home/homePageStyles.tsx` 当前约 463 行
  - `src/features/settings/permissions/PermissionsGovernanceControlsSection.tsx` 当前约 473 行
  - `src/hooks/useAskPrompt.tsx` 当前约 459 行
  - `src/hooks/useKnowledgeRuleSqlManager.ts` 当前约 423 行
  - `src/components/pages/home/promptThread/ChartAnswer.tsx` 当前约 500 行
  - `src/features/home/routes/HomeLandingPage.tsx` 当前约 284 行
  - `src/hooks/useHomeSidebar.tsx` 当前约 404 行
  - `src/features/settings/identity/ManageIdentityPage.tsx` 当前约 346 行
  - `src/features/settings/automation/ManageAutomationPage.tsx` 当前约 316 行
  - `src/features/settings/systemTasks/ManageSystemTasksPage.tsx` 当前约 310 行
  - `src/features/settings/users/UsersMembersSection.tsx` 当前约 280 行
  - `src/features/auth/AuthPage.tsx` 当前约 345 行
  - `src/features/settings/connectors/connectorsPageUtils.ts` 当前约 219 行
- `home/index.tsx` 已进一步收敛到 route-entry re-export，landing page 组合已迁入 `src/features/home/routes/HomeLandingPage.tsx`。
- `KnowledgeMainStage.tsx` 已进一步收敛到约 191 行，overview 资产画廊、workbench header、modeling stage、SQL/规则 section 装配，以及主 stage props 类型都已继续下沉到专用组件 / hooks；新增 `KnowledgeOverviewStage.tsx`（约 115 行）/ `KnowledgeModelingSection.tsx`（约 50 行）/ `KnowledgeSqlTemplatesStage.tsx`（约 100 行）/ `KnowledgeInstructionsStage.tsx`（约 104 行）/ `knowledgeMainStageTypes.ts`（约 60 行）承接对应 stage/type lane；随后再新增 `buildKnowledgeMainStageSectionProps.ts` 与对应测试，统一承接 header / overview / modeling / SQL / 分析规则五条 stage props 映射；本轮又继续把这组 builder 拆成 `buildKnowledgeMainStageOverviewSectionProps.ts`（约 100 行）/ `buildKnowledgeMainStageEditorSectionProps.ts`（约 84 行）/ `knowledgeMainStageSectionPropTypes.ts`（约 15 行）/ `knowledgeMainStageOverviewSectionPropTypes.ts`（约 61 行）/ `knowledgeMainStageEditorSectionPropTypes.ts`（约 79 行），并继续新增 `buildKnowledgeMainStageEditorsInput.ts`（约 67 行）与对应测试，下沉 main-stage → editors hook 的纯输入映射；当前 `buildKnowledgeMainStageSectionProps.ts` 已收口为约 10 行 barrel。
- `KnowledgeOverviewSection.tsx` 已进一步收敛到约 114 行，并新增 `KnowledgeOverviewStats.tsx`（约 59 行）/ `KnowledgeOverviewAssetsPanel.tsx`（约 82 行）/ `KnowledgeAssetDetailDrawer.tsx`（约 72 行）与对应测试；随后 `KnowledgeOverviewAssetsPanel.tsx` 又继续拆出 `KnowledgeAssetCreateCard.tsx`（约 34 行）/ `KnowledgeAssetGalleryCard.tsx`（约 89 行）/ `KnowledgeOverviewAssetsEmptyState.tsx`（约 49 行）/ `KnowledgeOverviewAssetsLoadingOverlay.tsx`（约 39 行），overview 的统计卡、资产画廊/空状态/加载态以及详情抽屉封装已进一步分层到独立 section 组件。
- `AssetDetailContent.tsx` 已进一步收敛到约 165 行，并新增 `AssetDetailFieldOverview.tsx`（约 105 行）/ `AssetDetailUsagePanel.tsx`（约 83 行）/ `buildAssetDetailFieldColumns.tsx`（约 103 行）/ `assetDetailContentTypes.ts`（约 17 行）；资产详情的字段概览、推荐问法面板与字段列定义已拆到独立展示片段/纯 helper。
- `pages/knowledge/index.tsx` 已进一步收敛到约 37 行，route entry 已基本只保留 helper re-export + page frame 调用；page-level 热点已基本收口回 controller/view-state 组合层。
- `useKnowledgeWorkbenchControllerDataState.ts` 当前约 35 行，并新增 `buildKnowledgeWorkbenchKnowledgeStateInputs.ts`（约 35 行）/ `buildKnowledgeWorkbenchContentDataInputs.ts`（约 44 行）/ `buildKnowledgeWorkbenchModelingStateInputs.ts`（约 29 行）/ `knowledgeWorkbenchControllerDataStateTypes.ts`（约 39 行）；knowledge/content/modeling data-state 输入组装已按 builder + shared types 分层。
- `useKnowledgeWorkbenchKnowledgeState.ts` 当前约 127 行，并新增 `knowledgeWorkbenchKnowledgeStateTypes.ts`（约 41 行）/ `buildKnowledgeWorkbenchBaseSelectionInputs.ts`（约 54 行）/ `buildKnowledgeWorkbenchBaseMetaInputs.ts`（约 64 行）/ `buildKnowledgeWorkbenchRuntimeBindingsInputs.ts`（约 38 行）/ `buildKnowledgeWorkbenchRuntimeContextInputs.ts`（约 25 行）/ `buildKnowledgeWorkbenchListCacheInputs.ts`（约 28 行）/ `buildKnowledgeWorkbenchSelectorFallbackInputs.ts`（约 36 行）/ `buildKnowledgeWorkbenchDataLoadersInputs.ts`（约 19 行）；selection/meta/runtime-binding/runtime-context/list-cache/selector-fallback/data-loader 输入组装已按 shared types + pure builders 分层。
- `useKnowledgeWorkbenchContentData.ts` 当前约 74 行，并新增 `knowledgeWorkbenchContentDataTypes.ts`（约 43 行）/ `buildKnowledgeWorkbenchConnectorsInputs.ts`（约 34 行）/ `buildKnowledgeWorkbenchDiagramInputs.ts`（约 24 行）/ `buildKnowledgeWorkbenchRuntimeDataSyncInputs.ts`（约 27 行）/ `buildKnowledgeWorkbenchAssetsInputs.ts`（约 32 行）；connectors / diagram / runtime-sync / assets 输入组装已按 shared types + pure builders 分层。
- `buildKnowledgeWorkbenchControllerViewInputs.ts` 当前约 36 行，`knowledgeWorkbenchControllerViewTypes.ts` 当前约 14 行，并新增 `knowledgeWorkbenchControllerViewOperationTypes.ts` / `knowledgeWorkbenchControllerViewStateInputTypes.ts` 承接 controller-interaction → view-state 的 operations/state 共享参数类型；builder 已进一步收口。
- `useKnowledgeWorkbenchControllerViewState.ts` 当前约 16 行，已收口为轻量 hook 委派层，不再属于 giant-file 热点。
- `knowledgeWorkbenchControllerInteractionTypes.ts` / `buildKnowledgeWorkbenchControllerInteractionOperationInputs.ts` / `buildKnowledgeWorkbenchControllerInteractionViewInputs.ts` 当前约 16 / 69 / 27 行；controller-interaction 的 operations/view 两段输入映射已继续拆成 types + 两条 lane，原本仅做 barrel re-export 的 `buildKnowledgeWorkbenchControllerInteractionInputs.ts` 已删除，hook / tests 已直接依赖真实 builder。
- `buildKnowledgeWorkbenchPageInteractionLocalInputs.ts` / `buildKnowledgeWorkbenchPageInteractionKnowledgeInputs.ts` / `buildKnowledgeWorkbenchPageInteractionContentInputs.ts` 当前已作为 page-interaction 的三条真实 builder lane 直接被入口与测试依赖，原本仅做 barrel re-export 的 `buildKnowledgeWorkbenchPageInteractionInputLanes.ts` 已删除。
- `useKnowledgeWorkbenchControllerInteractionState.ts` 当前约 38 行，已继续收口为轻量 hook 组合层，不再属于 giant-file 热点。
- `knowledgeWorkbenchControllerOperationsTypes.ts` / `buildKnowledgeWorkbenchControllerActionsInputs.ts` / `buildKnowledgeWorkbenchControllerRuleSqlInputs.ts` 当前约 61 / 61 / 23 行；controller-operations 的 actions/ruleSql 输入映射已继续拆成 types + 两条 lane，原本仅做 barrel re-export 的 `buildKnowledgeWorkbenchControllerOperationsInputs.ts` 已删除，controller-operations hook / tests 已直接依赖真实 builder。
- `useKnowledgeWorkbenchControllerOperations.ts` 当前约 27 行，已收口为轻量 hook 组合层，不再属于 giant-file 热点。
- `buildKnowledgeWorkbenchPageInteractionInputs.ts` 当前约 63 行，page-level input builder 已继续收口为轻量组合层。
- `knowledgeWorkbenchPageInteractionInputTypes.ts` 当前约 15 行，已收口为 aggregate barrel；page-interaction local/controller/data 三组共享类型已拆到独立 lane type 文件。
- `buildKnowledgeWorkbenchPageInteractionLocalInputs.ts` / `buildKnowledgeWorkbenchPageInteractionKnowledgeInputs.ts` / `buildKnowledgeWorkbenchPageInteractionContentInputs.ts` 当前约 67 / 79 / 64 行，page-level local/runtime/controller-data → controller-interaction 映射已继续按 lane 拆开。
- `useKnowledgeWorkbenchPageController.ts` 当前约 65 行，集中承接 knowledge workbench 的 page-level composition；controller-data / page-interaction builder 调用已继续下沉到 `useKnowledgeWorkbenchPageControllerData.ts` / `useKnowledgeWorkbenchPageControllerInteraction.ts` 后，剩余主要是 top-level hook composition wiring。
- `knowledgeWorkbenchPageControllerTypes.ts` / `buildKnowledgeWorkbenchPageControllerInteractionArgs.ts` / `buildKnowledgeWorkbenchPageStageArgs.ts` 当前约 22 / 72 / 46 行，page controller 的 interaction/stage 输入组装已按 types + lanes 分层；其中原本仅做 identity passthrough 的 `buildKnowledgeWorkbenchControllerDataInputs.ts` 与仅做 barrel re-export 的 `buildKnowledgeWorkbenchPageControllerInputs.ts` 都已删除，runtime/route 输入直接收口回 `useKnowledgeWorkbenchPageControllerData.ts`，page-controller 组合层也已改为直接依赖真实 builder lane。
- 已继续新增 `useKnowledgeWorkbenchPageControllerData.ts` / `useKnowledgeWorkbenchPageControllerInteraction.ts`（约 52 / 62 行），将 page controller 内剩余的 controller-data builder 调用与 page-interaction builder 调用继续下沉到独立组合 hook；page controller 主入口已进一步收口。
- `useKnowledgeWorkbenchPageInteractionState.ts` 当前约 17 行，已收口为轻量 hook 委派层，不再属于 giant-file 热点。
- `buildKnowledgeWorkbenchControllerStage.ts` 当前约 19 行，仅保留 page-stage 组装入口，不再属于 giant-file 热点。
- `buildKnowledgeWorkbenchControllerSidebarInput.ts` / `buildKnowledgeWorkbenchControllerMainStageInput.ts` / `buildKnowledgeWorkbenchControllerOverlaysInput.ts` 当前约 29 / 29 / 57 行，controller-stage 的 sidebar / main-stage / overlays 三条映射 lane 已拆成独立纯函数。
- 原本仅做 barrel re-export 的 `buildKnowledgeWorkbenchControllerStageInputs.ts` 已删除，`buildKnowledgeWorkbenchControllerStage.ts` 与对应测试已直接依赖三条真实 lane builder。
- 已继续新增 `buildKnowledgeWorkbenchMainStageOverviewInput.ts` / `buildKnowledgeWorkbenchMainStageEditorInput.ts`（约 77 / 63 行），将 main-stage lane 内的 overview/detail props 与 rule-sql/modeling props 继续拆成两条纯 builder lane；`buildKnowledgeWorkbenchControllerMainStageInput.ts` 已继续从约 72 行收口到约 29 行。
- `knowledgeWorkbenchControllerStageTypes.ts` 当前约 20 行，已收口为 barrel + aggregate args；stage local/knowledge/content/actions/ruleSql/view/modeling 共 7 份 lane type 文件独立承接共享输入定义。
- `useKnowledgeWorkbenchPresentationState.ts` 已进一步收口到约 28 行，并新增 `knowledgeWorkbenchPresentationStateTypes.ts`（约 67 行）、`buildKnowledgeAssetWorkbenchInputs.ts`（约 69 行）与 `buildKnowledgeWorkbenchNavigationStateInputs.ts`（约 35 行）承接共享参数类型与 asset/navigation 输入映射；hook 当前聚焦 asset workbench / navigation 纯展示态组合。
- `useKnowledgeWorkbenchViewState.ts` 已进一步收口到约 26 行，并新增 `knowledgeWorkbenchViewStateTypes.ts`（约 22 行）、`buildKnowledgeWorkbenchPresentationStateInputs.ts`（约 80 行）与 `buildKnowledgeWorkbenchSyncEffectsInputs.ts`（约 58 行）；hook 当前主要保留 sync effects + presentation-state 的轻量组合。
- `useKnowledgeWorkbenchModelingState.ts`（约 38 行）已承接 modeling summary + committed workspace key 这一组组合层，当前体量轻量。
- `home/[id].tsx` 已进一步收敛到约 380 行，polling / recovery orchestration 已下沉到 `useThreadRecoveryOrchestration.ts`，不再属于 giant-file 主热点。
- `useThreadRecoveryOrchestration.ts` 已进一步收敛到约 175 行，并新增 `threadRecoveryOrchestrationTypes.ts` / `threadRecoveryPlanHelpers.ts` / `threadRecoveryCleanupHelpers.ts` / `threadRecoveryQuestionStoreHelpers.ts` / `threadRecoveryPollingHelpers.ts`；原本一度拆出的 `buildThreadRecoveryOrchestrationResult.ts`、`buildThreadResponsePollingStartInputs.ts` / `buildThreadRecoveryPlanRunnerInputs.ts` / `buildThreadRecoveryCleanupInputs.ts` / `buildThreadResponsePollingSettleInputs.ts` / `buildThreadRecommendationPollingStateInputs.ts` 这批 identity passthrough builders 已删除，orchestration hook、effects 与 callbacks 当前直接调用真实 lane helper。
- 已新增 `useThreadRecoveryCleanupEffect.ts` / `useThreadRecoverySyncEffects.ts`（约 69 / 103 行），将 thread recovery hook 内的 cleanup lifecycle 与 response/recommend/question-store sync effects 继续下沉；`useThreadRecoveryOrchestration.ts` 已继续从约 175 行收口到约 133 行。
- 已继续新增 `threadRecoveryPlanHelpers.ts` / `threadRecoveryCleanupHelpers.ts` / `threadRecoveryQuestionStoreHelpers.ts` / `threadRecoveryPollingHelpers.ts`（约 49 / 38 / 28 / 75 行），将 recovery helper 再按 plan / cleanup / question-store / polling 四条 lane 分层。
- thread recovery 的 polling / cleanup / plan / question-store 相关 builders、effects、tests 当前已直接依赖各自 lane helper；原本仅做 barrel re-export 的 `threadRecoveryOrchestrationHelpers.ts` 已删除。
- 已继续新增 `useThreadResponsePollingStarter.ts` / `useThreadRecoveryPlanHandler.ts`（约 42 / 59 行），将 recovery orchestration entry hook 内剩余的 polling-start 与 recovery-plan callback 再下沉到独立 hook；`useThreadRecoveryOrchestration.ts` 已继续从约 133 行收口到约 97 行。
- thread recovery 一度新增 `useThreadRecoveryRefs.ts` / `useThreadRecoveryEffects.ts` / `useThreadRecoveryExecutionControls.ts` 承接 refs 初始化、cleanup+sync effects 调度，以及 polling-controls + recovery-plan-handler 的薄组合层；随后继续删除这三层仅做 passthrough 的 hook，`useThreadRecoveryOrchestration.ts` 当前直接使用 `useRef` 与真实 lane hooks。
- 已继续新增 `useThreadRecoveryResponseSyncEffect.ts` / `useThreadResponsePollingSettleEffect.ts` / `useThreadRecommendationPollingEffect.ts`（约 43 / 42 / 33 行），将 `useThreadRecoverySyncEffects.ts` 内的 response-sync、response-settle 与 recommendation-polling 三个 effect lane 继续拆到独立 hook；`useThreadRecoverySyncEffects.ts` 已继续从约 103 行收口到约 63 行。
- 已继续新增 `useThreadCreateResponseAction.ts`（约 98 行）与对应测试，将 thread response actions 中的 create-response / polling-handoff / optimistic hydration 编排从 `useThreadResponseActions.ts` 继续下沉；`useThreadResponseActions.ts` 已继续从约 289 行收口到约 240 行。
- 已继续新增 `useThreadRecommendedQuestionsAction.ts`（约 61 行）与对应测试，将 thread response actions 中的 recommendation-trigger / result-fetch / polling-stop 编排从 `useThreadResponseActions.ts` 继续下沉；`useThreadResponseActions.ts` 已继续从约 240 行收口到约 214 行。
- 已继续新增 `useThreadResponseArtifactActions.ts`（约 62 行）与对应测试，将 thread response actions 中的 view-create / sql-pair-create loading 与成功/失败反馈编排从 `useThreadResponseActions.ts` 继续下沉；`useThreadResponseActions.ts` 已继续从约 214 行收口到约 183 行。
- 已继续新增 `useThreadResponseMutationActions.ts`（约 110 行）与对应测试，将 thread response actions 中的 answer/chart generation、chart adjust、SQL fix + regenerate 编排从 `useThreadResponseActions.ts` 继续下沉；`useThreadResponseActions.ts` 已继续从约 183 行收口到约 105 行。
- 随后继续删除仅剩 page-level composition 的 `useThreadResponseActions.ts`，`pages/home/[id].tsx` 当前直接组合 create-response / recommendation / artifact / mutation 四条真实 lane；这一层已不再属于 giant-file / thin-hook 热点。
- `useKnowledgeWorkbenchEditors.tsx` 已进一步收敛到约 171 行，SQL / rule action lanes、dirty-guard 与 section switch 已拆到独立 hooks；当前主要保留 draft-state + published view-state 组合。
- 已继续新增 `resolveKnowledgeWorkbenchDraftDirty.ts`（约 23 行）与对应测试， 将 section switch / SQL 模板打开 / 分析规则打开三处重复的 draft-dirty 判定收敛到共享 helper；knowledge editors/action lanes 的重复判断噪音已继续下降。
- 已继续新增 `useKnowledgeWorkbenchSectionChangeGuard.ts`（约 55 行）与对应测试，将 editor rail 内的 section switch dirty-guard / drawer-close 编排从 `useKnowledgeWorkbenchEditors.tsx` 继续下沉。
- 已继续新增 `useKnowledgeWorkbenchAssetEditorLifecycle.ts`（约 77 行）与对应测试，将 SQL 模板 / 分析规则 action hooks 中重复的 reset / submit / close-drawer / apply-context 生命周期编排收敛到共享 hook；`useKnowledgeWorkbenchSqlActions.ts` / `useKnowledgeWorkbenchRuleActions.ts` 已继续收口到约 214 / 218 行。
- 已继续新增 `useKnowledgeWorkbenchEditorOpenAction.ts`（约 118 行）与对应测试，将 SQL 模板 / 分析规则 action hooks 中重复的 open-editor / section-switch / baseline-sync 编排收敛到共享 hook；`useKnowledgeWorkbenchSqlActions.ts` / `useKnowledgeWorkbenchRuleActions.ts` 已继续收口到约 193 / 197 行。
- 已继续新增 `knowledgeWorkbenchEditorOpenActionTypes.ts`（约 45 行）、`runKnowledgeWorkbenchEditorOpenEffects.ts`（约 58 行）与对应测试，将 `useKnowledgeWorkbenchEditorOpenAction.ts` 中长内联泛型参数类型与 open-editor 副作用执行段继续拆到 `types / effect` 两层；`useKnowledgeWorkbenchEditorOpenAction.ts` 已继续从约 118 行收口到约 89 行。
- 已继续新增 `useKnowledgeWorkbenchEditorEntryActions.ts`（约 99 行）与对应测试，将 SQL 模板 / 分析规则 action hooks 中重复的 create-from-asset / duplicate / delete-entry 编排收敛到共享 hook；`useKnowledgeWorkbenchSqlActions.ts` / `useKnowledgeWorkbenchRuleActions.ts` 已继续收口到约 167 / 173 行。
- 已继续新增 `knowledgeWorkbenchEditorEntryActionTypes.ts`（约 29 行）、`runKnowledgeWorkbenchEditorEntryEffects.ts`（约 124 行）与对应测试，将 `useKnowledgeWorkbenchEditorEntryActions.ts` 中长内联参数类型与 create-from-asset / duplicate / delete-entry 副作用执行段继续拆到 `types / effect` 两层；`useKnowledgeWorkbenchEditorEntryActions.ts` 已继续从约 99 行收口到约 87 行。
- 已继续新增 `useKnowledgeWorkbenchAssetEditorActions.ts`（约 156 行），将 SQL 模板 / 分析规则 action hooks 中共享的 lifecycle + open-editor + entry-actions 组合进一步收口到统一 hook；随后为保持 `KnowledgeMainStage.tsx` 的现有调用契约，`useKnowledgeWorkbenchSqlActions.ts` / `useKnowledgeWorkbenchRuleActions.ts` 补回 domain wrapper，当前约 142 / 148 行，仍明显低于收口前的 167 / 173 行。
- 已继续新增 `useKnowledgeWorkbenchEditorActions.ts`（约 139 行），将 editor rail 内的 dirty-guard / section-switch / sql-rule action lane 组合从 `useKnowledgeWorkbenchEditors.tsx` 继续下沉；`useKnowledgeWorkbenchEditors.tsx` 已继续从约 207 行收口到约 171 行。
- 已继续新增 `knowledgeWorkbenchEditorActionsHelpers.ts` 与对应测试，将 `useKnowledgeWorkbenchEditorActions.ts` 中长内联 props 类型、section-change guard/sql/rule lane input 映射、save-shortcut 输入与返回面组装继续下沉到纯 helper；`useKnowledgeWorkbenchEditorActions.ts` 已继续从约 139 行收口到约 79 行。
- 已继续把 editor-actions helper 按 `types / lane-input / result` 三层拆开：新增 `knowledgeWorkbenchEditorActionsTypes.ts`（约 81 行）、`buildKnowledgeWorkbenchEditorActionLaneInputs.ts`（约 109 行）、`buildKnowledgeWorkbenchEditorActionsResult.ts`（约 21 行），原 `knowledgeWorkbenchEditorActionsHelpers.ts` 已继续收口成约 15 行 barrel。
- `useKnowledgeWorkbenchEditorActions.ts` / `knowledgeWorkbenchEditorActionsHelpers.test.ts` 当前已直接依赖 `buildKnowledgeWorkbenchEditorActionLaneInputs.ts` / `buildKnowledgeWorkbenchEditorActionsResult.ts` / `knowledgeWorkbenchEditorActionsTypes.ts` 三条真实文件；原本仅做 barrel re-export 的 `knowledgeWorkbenchEditorActionsHelpers.ts` 已删除。
- 已继续新增 `useKnowledgeWorkbenchContextAssetState.ts`（约 53 行）与 `knowledgeWorkbenchContextAssetUtils.ts` / 对应测试，将 draft state 内的 context-asset 解析、无效选中清理与 selector options 组装从 `useKnowledgeWorkbenchDraftState.ts` 继续下沉；`useKnowledgeWorkbenchDraftState.ts` 已继续从约 190 行收口到约 175 行。
- 已继续新增 `useKnowledgeWorkbenchDraftDerivedState.ts`（约 99 行），将 draft state 内的 filtered list / dirty-flag memo 组合从 `useKnowledgeWorkbenchDraftState.ts` 继续下沉；`useKnowledgeWorkbenchDraftState.ts` 已继续从约 175 行收口到约 139 行。
- 已继续新增 `useKnowledgeWorkbenchDraftBaselineState.ts`（约 69 行）与对应测试，将 draft state 内的 baseline read/sync 编排从 `useKnowledgeWorkbenchDraftState.ts` 继续下沉；`useKnowledgeWorkbenchDraftState.ts` 已继续从约 139 行收口到约 102 行。
- 已继续新增 `useKnowledgeWorkbenchDraftUiState.ts`（约 27 行）与对应测试，将 SQL/规则列表筛选、scope/mode 与 drawer open 这些本地 UI state 从 `useKnowledgeWorkbenchDraftState.ts` 继续下沉；`useKnowledgeWorkbenchDraftState.ts` 当前约 108 行，职责已收口到 baseline/context/derived 三段组合。
- 已继续新增 `useKnowledgeWorkbenchDraftWatchValues.ts`（约 23 行）与对应测试，将 rule/sql form 的 `Form.useWatch` 读取从 `useKnowledgeWorkbenchDraftState.ts` 继续下沉；`useKnowledgeWorkbenchDraftState.ts` 当前约 113 行，职责已进一步收口到 ui-state/watch-values/baseline/context/derived 五段组合。
- 已继续新增 `knowledgeWorkbenchEditorsHelpers.ts`（当前约 7 行 barrel）与对应测试，并继续按 `types / input / result` 三层拆出 `knowledgeWorkbenchEditorsTypes.ts` / `buildKnowledgeWorkbenchEditorActionsInput.ts` / `buildKnowledgeWorkbenchEditorsResult.ts`（约 94 / 43 / 35 行），随后再补 `buildKnowledgeWorkbenchDraftStateInput.ts`（约 17 行）收走 draft-state input 子集；`useKnowledgeWorkbenchEditors.tsx` 已继续从约 124 行收口到约 28 行。
- `useKnowledgeWorkbenchEditors.tsx` / `buildKnowledgeWorkbenchDraftStateInput.ts` / `knowledgeWorkbenchEditorsHelpers.test.ts` 当前已直接依赖 `knowledgeWorkbenchEditorsTypes.ts` / `buildKnowledgeWorkbenchEditorActionsInput.ts` / `buildKnowledgeWorkbenchEditorsResult.ts` 三条真实文件；原本仅做 barrel re-export 的 `knowledgeWorkbenchEditorsHelpers.ts` 已删除。
- 已继续新增 `knowledgeWorkbenchAssetEditorActionConfigs.ts`（当前约 16 行 barrel）与对应测试，并继续按 lane 拆出 `knowledgeWorkbenchSqlAssetEditorActionConfigs.ts` / `knowledgeWorkbenchRuleAssetEditorActionConfigs.ts`；当前 SQL / Rule lane barrel 已继续收口到约 11 / 11 行，并再拆出 `knowledgeWorkbenchSqlAssetEditorActionTypes.ts` / `knowledgeWorkbenchRuleAssetEditorActionTypes.ts`（约 51 / 52 行）、`buildKnowledgeWorkbenchSqlAssetEditorActionsInput.ts` / `buildKnowledgeWorkbenchRuleAssetEditorActionsInput.ts`（约 93 / 98 行）、`buildKnowledgeWorkbenchSqlActionsResult.ts` / `buildKnowledgeWorkbenchRuleActionsResult.ts`（约 30 / 30 行），将 SQL 模板 / 分析规则 action hooks 中大段 asset-editor config、open-editor 参数映射与 domain 返回面彻底分到 `types / input / result` 三层；`useKnowledgeWorkbenchSqlActions.ts` / `useKnowledgeWorkbenchRuleActions.ts` 已继续收口到约 48 / 48 行。
- 已继续新增 `knowledgeWorkbenchAssetEditorActionsTypes.ts`（约 75 行）与 `buildKnowledgeWorkbenchAssetEditorActionsResult.ts`（约 28 行），将 `useKnowledgeWorkbenchAssetEditorActions.ts` 中长内联泛型 props 定义与返回面组装继续拆到 `types / result` 两层；`useKnowledgeWorkbenchAssetEditorActions.ts` 已继续从约 156 行收口到约 109 行。
- 已继续新增 `buildKnowledgeWorkbenchAssetEditorActionsInputs.ts`（约 155 行）与对应测试，将 `useKnowledgeWorkbenchAssetEditorActions.ts` 中 lifecycle/open-entry 三段子 hook 的长 input 映射继续拆成纯 builder；当前 `useKnowledgeWorkbenchAssetEditorActions.ts` 约 118 行，虽然较上一轮略回升，但主体已收口为薄 orchestration 层。
- 已继续把 asset-editor input builder 按 `lifecycle / open / entry` 三层拆开：新增 `buildKnowledgeWorkbenchAssetEditorLifecycleInput.ts`（约 48 行）、`buildKnowledgeWorkbenchAssetEditorOpenInput.ts`（约 55 行）、`buildKnowledgeWorkbenchEditorEntryActionsInput.ts`（约 54 行），原 `buildKnowledgeWorkbenchAssetEditorActionsInputs.ts` 已继续收口成约 3 行 barrel。
- `useKnowledgeWorkbenchAssetEditorActions.ts` / `buildKnowledgeWorkbenchAssetEditorActionsInputs.test.ts` 当前已直接依赖 `buildKnowledgeWorkbenchAssetEditorLifecycleInput.ts` / `buildKnowledgeWorkbenchAssetEditorOpenInput.ts` / `buildKnowledgeWorkbenchEditorEntryActionsInput.ts` 三条真实 builder lane；原本仅做 barrel re-export 的 `buildKnowledgeWorkbenchAssetEditorActionsInputs.ts` 已删除。
- 已继续新增 `useKnowledgeWorkbenchAssetEditorActions.test.tsx`，并将 `useKnowledgeWorkbenchAssetEditorActions.ts` 的 orchestration 输入收口为单一 `args` 对象；当前 hook 已继续从约 118 行收口到约 50 行，仅保留 lifecycle/open/entry 三段子 hook 的轻量组合与返回面装配。
- 已继续新增 `knowledgeWorkbenchDraftStateHelpers.ts`（约 120 行）与对应测试，将 `useKnowledgeWorkbenchDraftState.ts` 中 derived-state 输入映射与返回面组装下沉到纯 helper；`useKnowledgeWorkbenchDraftState.ts` 已继续从约 113 行收口到约 55 行。
- 已新增 `KnowledgeWorkbenchEditorRailControls.tsx` / `KnowledgeWorkbenchEditorEmptyState.tsx` / `KnowledgeWorkbenchAssetContextPanel.tsx` / `KnowledgeWorkbenchEditorDrawerFooter.tsx`（约 63 / 22 / 95 / 46 行），将 SQL 模板 / 分析规则 section 的共享 rail、空状态、参考资产面板与抽屉 footer 下沉到 editor fragments；`KnowledgeSqlTemplatesSection.tsx` / `KnowledgeInstructionsSection.tsx` 已继续从约 368 / 395 行收口到约 294 / 313 行。
- 已继续新增 `KnowledgeSqlTemplateList.tsx` / `KnowledgeSqlTemplateDrawer.tsx` / `KnowledgeInstructionList.tsx` / `KnowledgeInstructionDrawer.tsx`（约 167 / 118 / 176 / 132 行），将 SQL 模板 / 分析规则 section 再按 list vs drawer 两条 lane 分层；`KnowledgeSqlTemplatesSection.tsx` / `KnowledgeInstructionsSection.tsx` 已继续从约 294 / 313 行收口到约 119 / 119 行。
- 已继续新增 `KnowledgeSqlTemplateCardGrid.tsx` / `KnowledgeInstructionCardGrid.tsx`（约 120 / 128 行），将 list lane 再按 rail controls vs card collection 分层；`KnowledgeSqlTemplateList.tsx` / `KnowledgeInstructionList.tsx` 已继续从约 167 / 176 行收口到约 83 / 86 行。
- 已继续新增 `KnowledgeWorkbenchCreateEditorCard.tsx` / `KnowledgeSqlTemplateCard.tsx` / `KnowledgeInstructionCard.tsx`（约 33 / 92 / 97 行），将 card collection 再按共享 create card 与 domain item card 分层；`KnowledgeSqlTemplateCardGrid.tsx` / `KnowledgeInstructionCardGrid.tsx` 已继续从约 120 / 128 行收口到约 50 / 50 行。
- 已继续新增 `KnowledgeWorkbenchEditorDrawerShell.tsx` / `KnowledgeSqlTemplateFormFields.tsx` / `KnowledgeInstructionFormFields.tsx`（约 54 / 36 / 50 行），将 drawer lane 再按共享 drawer shell 与 domain form-fields 分层；`KnowledgeSqlTemplateDrawer.tsx` / `KnowledgeInstructionDrawer.tsx` 已继续从约 118 / 132 行收口到约 81 / 81 行。
- 已继续新增 `KnowledgeWorkbenchAssetContextEditorPanel.tsx`（约 61 行），将 SQL 模板 / 分析规则 drawer 中重复的参考资产 action 组装、suggested-question 回填与 readonly gating 继续下沉到 shared editor panel；`KnowledgeSqlTemplateDrawer.tsx` / `KnowledgeInstructionDrawer.tsx` 已继续从约 81 / 81 行收口到约 73 / 73 行。
- 已继续新增 `KnowledgeWorkbenchAssetEditorDrawer.tsx`（约 75 行），将 SQL 模板 / 分析规则 drawer 中重复的 drawer shell + asset-context panel 组合继续下沉到 shared asset-editor drawer；`KnowledgeSqlTemplateDrawer.tsx` / `KnowledgeInstructionDrawer.tsx` 已继续从约 73 / 73 行收口到约 69 / 69 行。
- 已继续新增 `KnowledgeWorkbenchEditorItemCard.tsx`（约 86 行），并将 `KnowledgeSqlTemplateCard.tsx` / `KnowledgeInstructionCard.tsx` 收口为 domain wrapper（约 44 / 47 行）；item card lane 已继续从约 92 / 97 行收口到更轻的 wrapper + shared shell 结构。
- 已继续新增 `KnowledgeWorkbenchEditorItemCardMeta.tsx` / `KnowledgeWorkbenchEditorItemCardActions.tsx`（约 32 / 51 行），将共享 item-card 的标题+状态 meta 与 duplicate/delete action wiring 再按 meta/actions 两条 lane 分层；`KnowledgeWorkbenchEditorItemCard.tsx` 已继续从约 86 行收口到约 63 行。
- 知识库页面相关的 route-builder 重复逻辑已下沉到 `src/utils/knowledgeWorkbench.ts`，后续 giant-file 热点更多集中在 orchestration 与 editor 状态，而非 URL 构建重复。
- `index.styles.ts` 当前已直接 re-export 到 workbench leaf style files；原本仅做多层 barrel re-export 的 `knowledgePageWorkbenchStyles.ts` / `knowledgePageWorkbenchShellStyles.ts` / `knowledgePageWorkbenchSummaryStyles.ts` / `knowledgePageWorkbenchSummaryCardStyles.ts` / `knowledgePageWorkbenchEditorStyles.ts` / `knowledgePageWorkbenchEditorCardStyles.ts` / `knowledgePageWorkbenchEditorItemStyles.ts` 已删除。
- `home/[id].tsx` 与 knowledge workbench styles 都已开始第二轮收口，不再是完全未动状态。
- `ChartAnswer.tsx` 已从 giant-file 热点列表退出：图表 field 对齐 / renderer 选择 / 完成态 helper 已下沉到 `chartAnswerUtils.ts`，pin-to-dashboard modal 与局部 styled shell 也已拆到独立文件；当前主组件约 500 行，达到本轮“500 行以内即可”的收口标准。
- `useKnowledgeRuleSqlManager.ts` 也已从 giant-file 热点列表退出：规则/SQL 模板的 form value 类型、缓存判定、payload builder 与列表匹配 helper 已下沉到 `knowledgeRuleSqlManagerUtils.ts`，当前主 hook 约 423 行，保留知识库工作台现有 imports 的兼容 re-export。
- `useAskPrompt.tsx` 同样已从 giant-file 热点列表退出：ask prompt 的完成态判定、推荐问题历史、thread cache update 与 selector 解析逻辑已下沉到 `askPromptUtils.ts`，当前主 hook 约 459 行，保留首页/线程页既有 helper exports。
- `homePageStyles.tsx` 与 `PermissionsGovernanceControlsSection.tsx` 这两处临界热点也已收口到 500 行以下：前者通过把 styled leaf 改为 inline exports 删除集中 export block，后者把 access review / break-glass 相关类型下沉到 `permissionsGovernanceControlTypes.ts`；两者当前约 463 / 473 行。
- `referenceDemoKnowledge.ts` 已从 giant-file 热点列表退出：样例知识库数据、资产别名与共享类型已下沉到 `referenceDemoKnowledgeData.ts` / `referenceDemoKnowledgeTypes.ts`，当前主文件约 165 行。
- `features/knowledgePage/lists.tsx` 也已从 giant-file 热点列表退出：SQL 模板 / 分析规则列表共用的虚拟滚动容器已下沉到 `virtualizedManageEntryList.tsx`，当前主文件约 459 行。
- `errorHandler.tsx` 也已从 giant-file 热点列表退出：operationName 映射与 fallback copy 已下沉到 `errorHandlerOperationHandlers.tsx`，主文件当前约 320 行，并有独立 handler 单测覆盖。
- `common.ts` 也已从 giant-file 热点列表退出：后台 tracker / schedule worker 初始化已下沉到 `commonBackgroundTrackers.ts`，版本化 singleton 逻辑已下沉到 `commonComponentSingleton.ts`；当前主文件约 494 行，聚焦组件装配主流程。
- `components/learning/guide/stories.tsx` 也已从 giant-file 热点列表退出：四条引导 story 已拆到 `guideFlowStories.tsx` / `knowledgeGuideStories.tsx`，共享 preview panel、language switcher 与 driver config 已下沉到 `storyShared.tsx` / `storyTypes.ts`；当前入口文件约 30 行，仅保留 player dispatch。
- `home/[id].tsx` 已进一步收敛到约 380 行，page shell、conversation shell、modal overlays 与 recovery orchestration 已下沉到 thread feature 组件 / hooks。
- `home/dashboard.tsx` 已进一步收敛到 route-entry re-export；dashboard workbench 页面组合当前已下沉到 `features/home/dashboard/ManageDashboardPage.tsx`，为后续继续拆 dashboard rail / stage / 请求编排提供稳定 feature 边界。
- `workspace.tsx` 已进一步收敛到约 452 行，不再属于 giant-file 热点。
- `settings/connectors.tsx` 已进一步收敛到 route-entry re-export；连接器页 shell、catalog 与 modal 组合当前已下沉到 `features/settings/connectors/ManageConnectorsPage.tsx`。
- `settings/users.tsx` 也已进一步收敛到 route-entry re-export；用户管理页当前直接由 `features/settings/users/ManageUsersPage.tsx` 承担 shell 与成员管理组合。
- `settings/audit.tsx` / `settings/diagnostics.tsx` / `settings/identity.tsx` / `settings/automation.tsx` / `settings/system-tasks.tsx` / `settings/platform.tsx` 也已进一步收敛到 route-entry re-export；对应治理/诊断/身份/自动化/系统任务/平台页的页面实现均已下沉到 `features/settings/*/Manage*Page.tsx`。
- `settings/skills.tsx` 已进一步收敛到 route-entry re-export；skills 页的 shell、capability 解析与 modal/section 组合当前已下沉到 `features/settings/skills/ManageSkillsPage.tsx`，原本仅做 page-level 组合的 `useManageSkillsPage.ts` 已删除。
- `settings.tsx` 已进一步收敛到 route-entry re-export；个人资料页的 shell、代理登录提示与密码修改页面组合当前已下沉到 `features/settings/profile/ManageProfilePage.tsx`，并直接复用 `features/settings/settingsShell.ts` 的共享导航壳。
- `auth.tsx` 已进一步收敛到 route-entry re-export；统一登录页与 redirect helper 当前已下沉到 `features/auth/AuthPage.tsx`，页面入口仅保留 re-export 以维持测试与 compatibility alias 的既有导出契约。
- `settings/permissions.tsx` 也已进一步收敛到 route-entry re-export；权限页当前直接由 `features/settings/permissions/ManagePermissionsPage.tsx` 承担 shell + role-catalog 组合，原本仅做 page-level composition 的 `useManagePermissionsPage.ts` 已删除，且未在当前 UI 中使用的 governance / impersonation explain lane 不再在页面入口实例化。
- connectors 页一度新增 `useManageConnectorsPage.ts` 承接 runtime-state / editor-state / catalog / secret / mutation / control 的 page-level 组合；随后继续删除这层仅做 composition 的 hook，并让 `ManageConnectorsPage.tsx` 直接依赖真实 lane。更早一轮引入的 `useManageConnectorsRuntimeContext.ts` / `useManageConnectorsPageOperations.ts` 这两层薄组合也已在此前删除。
- `useConnectorMutationOperations.ts` 已继续收口到约 58 行，并新增 `connectorMutationOperationTypes.ts` / `useConnectorSubmitOperation.ts` / `useConnectorDeleteOperation.ts`（约 18 / 95 / 47 行）；save/delete orchestration 已继续拆到独立 lane，连接测试链路仍由 `useConnectorTestingOperations.ts` 承接。
- `buildKnowledgeWorkbenchStageProps.ts` 当前已收口为共享 props 类型文件，不再包含 main-stage/sidebar/overlay identity helper；`historicalSnapshotReadonlyHint` 的注入与 sidebar/overlay 直传逻辑都已直接收口在 `buildKnowledgeWorkbenchPageStage.ts`，stage props 这一层的无意义中转已进一步下降。
- 但上述文件仍明显偏大，后续继续拆分会降低上下文负担与回归风险。
