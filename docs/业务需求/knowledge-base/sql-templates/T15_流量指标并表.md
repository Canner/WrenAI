---
kb_asset_type: sql_template
import_target: sql_pair
import_format_version: v1
dialect: tidb_mysql8
parameter_style: colon_named
result_grain: biz_date + channel_id
id: T15
title: 流量指标并表
report: 综合日报表
priority: high
status: blocked_missing_source
source_tables:
  - 外部流量表（待补）
parameters:
  - tenant_plat_id
  - channel_id
  - biz_date
question_variants:
  - 按渠道/日期把 PV/UV/下载点击UV 并入综合日报
source_documents:
  - 第一期数据报表需求V1.xlsx
  - 数据报表API对应SQL&DSL语句整理.xlsx
---

# T15 流量指标并表

## 模板用途

按渠道/日期把 PV/UV/下载点击UV 并入综合日报。

## 建议问题（可转为 sql_pair.question）

- 按渠道/日期把 PV/UV/下载点击UV 并入综合日报

## 核心表/模型

- 外部流量表（待补）

## 参数

- tenant_plat_id
- channel_id
- biz_date

## SQL 模板

```sql
-- BLOCKED: 缺 PV / UV / 下载点击UV 数据源
```

## 备注

- 当前缺源，先占位。
- 当前缺少外部数据源或 SQL 化数据模型，暂不能形成可执行 SQL pair。
