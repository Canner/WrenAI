
- 增加多租户，用户管理/权限管理，隔离叫做 workspace
- 增加知识库管理，每个workspace下可以有多个知识库，每个知识库可以有多数据源，用户可以切换项目进行问数和报表生成
- 增加术语表，在 NL2SQL 阶段可以让 sql 更加准确
- 项目默认采用 Postgresql 和 postgresql 自带的向量库做存储
- 使用 deepagents + skill ，改变原有的流程，让查询时可以先去用 skill 进行数据查询，如果查不到再去走 WrenAI 这套NL2SQL体系
- 采用 deepagents +skill 我们要看看 Haystack 和 Hamilton 是否还需要，这个需要讨论
- deepagents 的 memory 是否需要使用，这个需要讨论
- Dashboard 可以有多个看板
- 增加数据权限控制，需要做到可以按照项目隔离，或者按照特定的数据行/列级别的隔离
- 增加定时任务，可以定时生成报表


