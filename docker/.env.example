# Project Configuration
COMPOSE_PROJECT_NAME=wren
PLATFORM=linux/amd64
PROJECT_DIR=.

# Service Ports
WREN_ENGINE_PORT=8080          # Wren Engine API port
WREN_ENGINE_SQL_PORT=7432      # Wren Engine SQL service port
WREN_AI_SERVICE_PORT=5555      # Wren AI service port
WREN_UI_PORT=3000              # Wren UI port
IBIS_SERVER_PORT=8000          # Ibis Server port

# Service Endpoints (for docker-compose-dev.yaml)
WREN_UI_ENDPOINT=http://docker.for.mac.localhost:3000  # Wren UI service endpoint

# LLM (Language Learning Model) Settings
LLM_OPENAI_API_KEY=          # Your OpenAI API Key (required)
EMBEDDER_OPENAI_API_KEY=     # Your OpenAI Embedding API Key (required)
# Supported models: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
GENERATION_MODEL=gpt-4o-mini  # Default LLM model used for generation tasks

# Version Information
# Update these to the latest versions as needed
WREN_PRODUCT_VERSION=0.9.0-rc.5
WREN_ENGINE_VERSION=0.10.4
WREN_AI_SERVICE_VERSION=0.8.19
IBIS_SERVER_VERSION=sha-d9f5ebf
WREN_UI_VERSION=0.13.2
WREN_BOOTSTRAP_VERSION=0.1.5

# AI Service Related Configuration
AI_SERVICE_ENABLE_TIMER=       # Enable or disable AI service timer (true/false)
AI_SERVICE_LOGGING_LEVEL=INFO  # Logging level for AI service (DEBUG, INFO, WARN, ERROR)
SHOULD_FORCE_DEPLOY=1          # Force deploy AI service (1 for yes, 0 for no)
QDRANT_HOST=qdrant             # Host for Qdrant (vector search engine)

# User Configuration (UUID v4)
USER_UUID=                     # User's unique identifier (optional, auto-generated if empty)

# Telemetry and Analytics
POSTHOG_API_KEY=phc_nhF32aj4xHXOZb0oqr2cn4Oy9uiWzz6CCP4KZmRq9aE  # PostHog API Key for tracking
POSTHOG_HOST=https://app.posthog.com  # PostHog service endpoint
TELEMETRY_ENABLED=true                # Enable telemetry data collection (true/false)

# Host Port Configuration (optional)
# Change these if you have port conflicts on your host machine
HOST_PORT=3000              # Port exposed to the host for Wren UI
AI_SERVICE_FORWARD_PORT=5555 # Port exposed to the host for Wren AI service
