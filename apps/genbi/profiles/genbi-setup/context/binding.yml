# Bound to a RAW source (raw/schema.json), not an existing wren project — at connect time no wren
# project exists yet (the precondition inversion this profile exists to handle, landing on the skill
# family — see profile.yml).
# Resolved relative to THIS Warble project's own root (genbi-setup/), i.e. this points at
# genbi-setup/raw/ (a deliberately empty placeholder, see raw/schema.json), never at whatever project
# the onboarding flow itself creates on disk at runtime.
kind: raw_source
project: raw
