name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    uses: Ai-road-4-You/enterprise-ci-cd/.github/workflows/release-tag-only.yml@v1
    secrets: inherit
