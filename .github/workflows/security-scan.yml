name: Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 4 * * 1'

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: Ai-road-4-You/enterprise-ci-cd/.github/workflows/security-scan.yml@v1
    secrets: inherit

