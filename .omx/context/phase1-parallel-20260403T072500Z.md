# Phase1 parallel launch context

## Task statement
Use OMX parallel execution to start the next Phase 1 implementation lanes.

## Desired outcome
Run three parallel lanes with bounded ownership:
1. P1-09 deepagents ask hardening
2. P1-10 PostgreSQL + pgvector default cutover
3. P1-02 auth/session minimal closure

## Known facts
- P1-03/P1-04/P1-08/P1-11 are already substantially advanced in the working tree.
- P1-09 is in progress with deepagents_orchestrator/tool_router/legacy fallback already present.
- P1-10 is still not switched by default; docker compose/config and knex still point to sqlite/qdrant.
- P1-02 has repositories/services/tests but no auth API routes yet.

## Constraints
- Keep changes bounded per lane.
- Do not revert unrelated working-tree edits.
- Avoid shared-file collisions where possible.
- .omx/plans/* are read-only.

## Open questions
- How far each lane can get before hitting shared integration points.
- Whether P1-02 needs minimal UI follow-up after API closure.

## Likely touchpoints
- P1-09: wren-ai-service/src/core/*, wren-ai-service/src/web/v1/services/ask.py, related tests
- P1-10: docker/docker-compose.yaml, docker/config.example.yaml, wren-ui/src/apollo/server/utils/knex.ts, pgvector provider/tests
- P1-02: wren-ui/src/pages/api/auth/*, auth/workspace services/repos/context/tests
