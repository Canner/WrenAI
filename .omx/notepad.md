

## WORKING MEMORY
[2026-04-01T04:16:46.885Z] 完成 ralplan 计划落盘：.omx/plans/ralplan-wrenai-v1-rebuild.md。关键补强：1) workspace/knowledge_base/runtime project 三层边界；2) deepagents/Haystack/Hamilton 职责收敛；3) connector+asset+capability 模型；4) 权限三层执行；5) PostgreSQL metadata 与 pgvector 默认切换拆阶段；6) dashboard refresh 绑定 runtime project。已验证 99 个 file:line 引用对应 33 个文件全部存在。

[2026-04-01T04:18:01.692Z] 2026-04-01: 完成 WrenAI V1 改造 ralplan，输出 .omx/plans/ralplan-wrenai-v1-rebuild.md。关键收敛：1) workspace / knowledge_base / runtime project 三层边界；2) 保留 Wren Engine，deepagents 前置编排；3) metadata PostgreSQL 一期必须，pgvector provider parity 后切默认；4) 权限三段式 enforcement。
[2026-04-01T05:52:04.453Z] 2026-04-01: 用户已确认 WrenAI V1 方向：内建账号优先、产品层去掉 Project 仅保留知识库、V1 API source 默认按 REST JSON connector、定时报表仅站内、权限先做 workspace+knowledge_base、存储直接 PostgreSQL+pgvector、deepagents 尽快替换 ask orchestration。接下来据此修订 ralplan。
[2026-04-01T08:37:57.100Z] Cleanup plan for current-project migration continuation: (1) remove remaining main-path current-project dependency in dashboard resolver/service callsites by passing runtimeScope projectId explicitly; (2) tighten askingService explicit-project callsites/signatures where only resolver callers remain; (3) add focused tests for explicit projectId path / bootstrap compatibility if needed; (4) run rg + LSP + targeted Jest to prove no new regressions.
[2026-04-01T08:48:14.080Z] Current-project migration batch complete: dashboardService/resolver now require explicit projectId; askingService createThread/listThreads/createInstantRecommendedQuestions now require explicit projectId; projectService getProjectDataSource* and recommendation methods tightened to explicit project/projectId; added resolver unit test for bootstrap fallback. Remaining getCurrentProject usage intentionally limited to projectResolver compatibility helpers + projectService shim.
[2026-04-01T09:00:45.681Z] P1-04 adjacent hardening batch done: added graphql bootstrap context tests; added getScopedThreadHistories helper so REST/stream ask + generate_sql history lookup now filters by both projectId and threadId; apiHistory GraphQL query now always scopes to ctx.runtimeScope.project.id and rejects mismatched projectId filter. Verification: 8 targeted suites, 72 tests passed.
[2026-04-01T09:12:55.148Z] Next migration slice plan: (1) add nullable runtime identity columns to api_history and asking_task via one reversible migration; (2) centralize api_history record creation so REST + stream endpoints persist runtime binding from RuntimeScope; (3) thread candidate asking_task creation persists runtime binding from ctx.runtimeScope via AskingPayload/AskingTaskTracker; (4) add focused unit tests for apiUtils record creation and askingTaskTracker persistence, then run targeted Jest/LSP.
[2026-04-01T09:37:41.265Z] P1-04 runtime identity persistence batch complete: added migration 20260401170000 for api_history + asking_task runtime columns; apiUtils now centralizes createApiHistoryRecord and all v1/stream endpoints pass runtimeScope; askingResolver/AskingTaskTracker persist runtime binding into asking_task on ask candidate creation/rerun. Verified with node -c migration, LSP 0 errors on touched files, and 9 targeted Jest suites / 76 tests passing.
[2026-04-01T09:53:00.864Z] [P1-04] Completed thread + adjustment runtime identity persistence slice: added thread runtime identity migration and repository fields; createThread now persists runtime identity; adjustment create/rerun flows persist runtime identity into asking_task; added focused tests for createThread and AdjustmentBackgroundTaskTracker. Verified with node -c migration, LSP 0 errors on touched files, and 6 targeted Jest suites / 21 tests passed.
[2026-04-01T10:07:17.438Z] [P1-04] Completed thread_response + answer runtime binding slice: added migration 20260401172000 for thread_response runtime columns with backfill from thread; thread/createThreadResponse/adjust-with-sql/adjustment-created responses now persist runtime identity; follow-up createAskingTask now reuses persisted thread deploy/runtime binding; previewData/previewBreakdownData and TextBasedAnswerBackgroundTracker now resolve deployment by persisted deployHash via DeployService.getDeployment. Verified with node -c on thread/thread_response migrations, LSP 0 errors on touched files, and 7 targeted Jest suites / 24 tests passed.
[2026-04-01T10:22:01.403Z] [P1-04] Added scope guards and scoped history filtering: AskingService now exposes assertThreadScope/assertResponseScope and filters listThreads by runtime identity; asking/model/dashboard resolvers and streaming_answer endpoint enforce current runtime scope before accessing existing thread/response records; API history thread lookups now filter by runtime identity instead of only projectId+threadId. Verified with node -c thread_response migration, LSP 0 errors on touched files, and 7 targeted Jest suites / 28 tests passed.
[2026-04-01T11:21:58.251Z] [P1-03/P1-04 UI propagation] 完成前端 runtime selector 透传：新增 wren-ui/src/apollo/client/runtimeScope.ts，Apollo client 通过 x-wren-* 头全局注入 workspace/knowledgeBase/kbSnapshot/deployHash/project；EventSource 流式请求改为自动拼接 runtime selector query params；选择器优先取当前 URL，命中后写入 sessionStorage/localStorage 以支持路由切换后继续沿用上下文。验证：LSP 5 文件 0 errors；Jest 2 suites/9 tests passed（runtimeScope + graphql bootstrap）。
[2026-04-01T11:24:51.614Z] 补充一处 runtime identity 收口：/api/ask_task/streaming 现在在转发 Wren AI SSE 之前先 resolveRequestScope，并用 askingService.assertAskingTaskScope(queryId, runtimeIdentity) 校验 queryId 所属 asking_task 的 runtime identity；askingService.test 新增对应拒绝跨 scope 用例。验证更新：Jest 3 suites / 22 tests passed。
[2026-04-01T22:15:22.823Z] [P1-03 UI routing] 继续把 runtime selector 从请求透传补到页面导航：新增 useRuntimeScopeNavigation hook（基于当前 route query，空时回退 storage），并把 HeaderBar、Home/Knowledge/API 侧栏、Home thread/dashboard 跳转、Modeling 打开 view metadata、onboarding/setup/settings 内部跳转、View 链接等关键入口改为保留 workspaceId/knowledgeBaseId/kbSnapshotId/deployHash/projectId。runtimeScope.test 补了 read-from-object/build-query 用例。验证：wren-ui LSP 仅剩既有 vega-lite 3 个历史错误；Jest 3 suites/24 tests passed。
[2026-04-01T23:57:43.459Z] 2026-04-02: Implemented runtime selector control plane for KB/snapshot switching. Added GraphQL query `runtimeSelectorState`, context plumbing for knowledgeBase/kbSnapshot repos, header `RuntimeScopeSelector` UI, and guarded thread route by disabling selector there. Selector changes now use full-page navigation with new runtime-scope query params to avoid Apollo cache cross-scope contamination.
[2026-04-02T00:17:32.225Z] 2026-04-02: Replaced runtime-scope selector full-page reload with SPA transition. Added `useRuntimeScopeTransition` (router.push + Apollo resetStore), `_app` component keying by runtime-scope state key, and client helpers `omitRuntimeScopeQuery`, `readRuntimeScopeSelectorFromUrl`, `buildRuntimeScopeStateKey` so scope changes remount pages and refetch data without stale cross-scope cache.
[2026-04-02T01:03:54.420Z] 2026-04-02: Closed remaining runtime-scope consistency hole in ask/task flows. AskingResolver now scope-checks task-based operations (`getAskingTask`, `cancelAskingTask`, `createThread` from taskId, `createThreadResponse` from taskId, `cancel/getAdjustmentTask`) against current runtime identity. Also aligned ThreadTree 'New' action to runtimeScopeNavigation. Added resolver tests for these guards.
[2026-04-02T05:38:49.401Z] 已完成 wren-ai-service AskService 最小 skill-first -> fallback NL2SQL 接线：新增 AskRequest skill/runtime 字段、AskResultResponse.skill_result 与 SKILL type，接入 skill_runner_client 并在 historical miss 后优先跑 skill；失败/缺失则回退原 NL2SQL。新增 tests/pytest/services/test_ask_skill_runner.py，目标 pytest 14 passed，compileall 通过。
[2026-04-02T06:05:09.628Z] 已完成 wren-ui 最小 skill ask 接线：新增 askContext helper 按 KB 解析 skill binding + resolved connector/secret，AskInput/WrenAIAdaptor 透传 runtime_identity/actor_claims/connectors/secrets/skills，REST ask/stream-ask 消费 SKILL 结果，AskingService createAskingTask 也会透传 skill 上下文；GraphQL AskingTask schema/resolver 增加 SKILL 与 skillResult。验证：wren-ui 5 suites/37 tests passed，TS diagnostics 0 error；wren-ai-service 14 passed，compileall 通过。

[2026-04-02T06:36:29.032Z] 2026-04-02 handoff: local code committed as ea4f7ae0. Repo status only has untracked .omx/. Current ~/.codex/config.toml uses model_provider=aihezu, base_url=https://cc.aihezu.dev/openai, wire_api=responses, disable_response_storage=true. If switching LiteLLM provider/base_url/key and seeing invalid_encrypted_content, do not resume the old remote conversation; open a new Codex session in /Users/liyi/Code/WrenAI and continue from commit ea4f7ae0 + .omx plans.
[2026-04-02T08:33:28.892Z] 2026-04-02: 完成 P1-11 ask/chart golden regression baseline。新增 tests/data/ask_golden_cases.json + chart_golden_cases.json，新增 test_ask_golden_regression.py / test_chart_golden_regression.py，覆盖 historical / sql-pairs / instructions / nl2sql / correction / general / followup / skill-first + chart generation。验证：PYTHONPATH=. poetry run pytest tests/pytest/services/test_ask_golden_regression.py tests/pytest/services/test_chart_golden_regression.py tests/pytest/services/test_ask_skill_runner.py tests/pytest/services/test_runtime_identity_bridge.py -q -> 16 passed；bash misc/scripts/scan-current-project.sh -> pass.
[2026-04-02T08:40:01.802Z] 2026-04-02: 继续补 Phase P1-11 验证面。新增 dashboardCacheBackgroundTracker.test.ts，验证 dashboard refresh 使用 dashboard.projectId 自身绑定的 project/deployment，而不是 current project；新增 secretService 两个 contract tests，覆盖 explicit keyVersion 写入/解密和错误主密钥解密失败。验证：cd wren-ui && yarn test src/apollo/server/backgrounds/tests/dashboardCacheBackgroundTracker.test.ts src/apollo/server/services/tests/dashboardService.test.ts src/apollo/server/services/tests/secretService.test.ts --runInBand -> 55 passed.
[2026-04-02T09:01:34.199Z] 2026-04-02: 完成 wren-ui 离线重加密能力。新增 src/apollo/server/services/secretReencrypt.ts、scripts/re_encrypt_secrets.ts、secretReencrypt.test.ts，并在 package.json 增加 re-encrypt-secrets 脚本。支持 dry-run/execute、workspace/scope/sourceKeyVersion 过滤、SOURCE_/TARGET_ encryption material 分离。验证：secretService+secretReencrypt Jest 8 passed；eslint pass；yarn re-encrypt-secrets --help 可运行。全量 yarn check-types 仍有既有历史错误（vega-lite/types 与页面 Props/IContext），新文件 LSP diagnostics 0 errors。
[2026-04-02T09:10:53.819Z] 2026-04-02: 补 schedule binding 基线。新增 backgrounds/scheduleWorker.ts + tests/scheduleWorker.test.ts，验证 DB-backed schedule job 只使用 job 自身 runtime binding（workspace/knowledgeBase/kbSnapshot/deployHash），并写入 schedule_job_run/audit_event；缺少 executor 时记录 failed run/audit。验证：dashboardCacheBackgroundTracker + scheduleWorker 相关 Jest 4 passed；eslint pass；scheduleWorker.ts LSP diagnostics 0 errors。
[2026-04-02T09:33:37.439Z] 完成 pgvector provider skeleton：新增 lazy-import 的 PgvectorProvider，不新增依赖；provider loader 已注册 pgvector；新增 smoke tests 覆盖 generate_components 接受 pgvector、lazy import 成功路径、缺失依赖清晰报错。验证：poetry run pytest tests/pytest/providers/test_loader.py tests/pytest/providers/test_providers.py tests/pytest/providers/test_pgvector_provider.py -q -> 7 passed。
[2026-04-02T09:51:38.869Z] 继续 P1-08 AI service runtime identity 收口：chart/chart_adjustment/sql_corrections/question_recommendation/instructions/sql_pairs 现在统一通过 BaseRequest.resolve_project_id() 做 runtime identity -> legacy project bridge，不再直接读 request.project_id。新增 runtime_identity_bridge 针对这些服务的 focused tests。验证：poetry run pytest tests/pytest/services/test_runtime_identity_bridge.py tests/pytest/services/test_chart_golden_regression.py -q -> 10 passed；poetry run python -m py_compile <touched files> 通过。
[2026-04-02T09:57:44.044Z] 继续补完 P1-08 service 层剩余收口：ask_feedback 也已改为 resolve_project_id()，现在 web/v1/services 下不再直接从 request.project_id/input.project_id 取主路径运行态。新增 ask_feedback bridge test。重新验证：pytest runtime_identity_bridge + chart_golden -> 11 passed；py_compile 通过。当前剩余 web/router 侧显式 project_id 主要只剩 semantics delete query param 入口与 relationship_recommendation 兼容传参。
[2026-04-02T10:10:37.584Z] 继续收口 router/adaptor 剩余显式 project_id 入口：AI service 新增 DeleteSemanticsRequest，/v1/semantics 支持 DELETE body + 兼容 query param，并统一走 resolve_project_id(); relationship_recommendation router 改为透传 resolved project/runtime_identity/configurations/request_from。Wren UI adaptor delete 改为发送 body（project_id + runtime_identity），ProjectResolver.resetCurrentProject 传 persisted runtime identity。新增 AI service route bridge tests + Wren UI adaptor/projectResolver tests。验证：pytest test_runtime_identity_bridge 12 passed；py_compile 通过；wren-ui targeted jest 17 passed；targeted eslint 通过。
[2026-04-02T10:14:51.555Z] Cleanup plan for next P1-08 inner-layer slice: (1) map ask-path pipelines/retrieval/indexing places still keyed by project_id; (2) add one shared runtime-scope helper instead of ad-hoc project_id filter construction; (3) update only ask-path critical pipelines first (historical/sql_pairs/instructions/db_schema + common metadata helper) while preserving project_id backward compatibility; (4) add focused tests proving runtime-aware filter construction and no ask-path regression; (5) run targeted pytest + py_compile before moving to the next cluster.
[2026-04-02T10:20:53.893Z] 2026-04-02 P1-08 inner slice completed: extracted build_runtime_scope_filters/normalize_runtime_scope_id into wren-ai-service/src/pipelines/common.py, migrated retrieval/historical_question_retrieval.py, retrieval/sql_pairs_retrieval.py, retrieval/instructions.py to use helper, and added focused retrieval scope tests. Verified with pytest (28 passed across retrieval scope + runtime identity bridge + ask golden regression) and py_compile.
[2026-04-02T10:28:35.620Z] 2026-04-02 P1-08 next slice completed: removed remaining hand-written legacy project_id filters from src/pipelines/retrieval/db_schema_retrieval.py, src/pipelines/generation/intent_classification.py, src/pipelines/indexing/__init__.py, src/pipelines/indexing/instructions.py, and src/pipelines/indexing/sql_pairs.py by reusing build_runtime_scope_filters(). Verified no raw 'field: project_id' filters remain under src/pipelines/{retrieval,indexing,generation}. Tests: pytest retrieval scope + indexing unit + runtime identity bridge + ask golden (38 passed), py_compile touched files.
[2026-04-02T10:35:25.016Z] 2026-04-02 P1-08 continued: metadata-based retrieval/generation pipelines now normalize runtime scope before use. Added retrieve_data_source() helper in src/pipelines/common.py and updated sql_functions, sql_knowledge, sql_generation, sql_correction, followup_sql_generation, sql_regeneration to pass normalized scope into pipeline inputs/post-process; dry-plan data_source lookup now goes through helper. Added tests in tests/pytest/pipelines/test_runtime_scope_context.py plus retrieve_data_source coverage. Verified with pytest (45 passed) and py_compile.
[2026-04-02T10:40:35.806Z] 2026-04-02 P1-08 indexing slice completed: added build_runtime_scope_meta() in src/pipelines/common.py and normalized runtime scope handling in indexing/project_meta.py, indexing/table_description.py, indexing/historical_question.py, indexing/db_schema.py across chunk/meta construction, clean(), and run() inputs/logging. Added focused normalization tests for chunkers and indexing run entrypoints. Verified with targeted pytest (64 passed) and py_compile. No remaining raw 'Project ID:' / direct project_id passthrough in those four indexing modules.
[2026-04-02T22:20:18.934Z] 2026-04-03 P1-08 semantic naming cleanup slice completed: normalized runtime scope handling in retrieval/historical_question_retrieval.py, retrieval/instructions.py, retrieval/sql_pairs_retrieval.py, retrieval/db_schema_retrieval.py, retrieval/sql_executor.py, generation/intent_classification.py, generation/utils/sql.py, indexing/instructions.py, indexing/sql_pairs.py, plus DocumentCleaner log normalization. Added retrieval/indexing run-normalization tests, converter meta normalization tests, SQLExecutor run normalization, and SQLGenPostProcessor engine execute normalization. Verified pytest targeted suite (61 passed) and py_compile. No remaining raw 'Project ID:' / direct unnormalized passthrough patterns under src/pipelines/{retrieval,generation,indexing} for the checked grep patterns.
[2026-04-02T22:30:20.638Z] 2026-04-03: P1-08 runtime identity boundary收口继续完成。wren-ai-service 已将 WrenUI provider、question_recommendation service、semantics_preparation service/router 内部统一到 runtime_scope 语义；新增 providers/test_wren_engine.py 覆盖 GraphQL projectId 归一化。验证：py_compile 通过；pytest 覆盖 providers+runtime_identity_bridge+ask/chart golden+runtime_scope_context，共 38 passed。
[2026-04-02T22:38:04.511Z] 2026-04-03: 继续 P1-09 前置接线。wren-ai-service 新增 Settings.ask_runtime_mode / ASK_RUNTIME_MODE（legacy|deepagents），AskService 仅在 deepagents 模式下启用 skill_runner-first，globals 已接入；ask skill runner/golden/config 测试已更新。验证：py_compile 通过；pytest(test_config + ask_skill_runner + ask_golden_regression + runtime_identity_bridge + wren_engine + runtime_scope_context) 44 passed。
[2026-04-03T01:58:34.290Z] 2026-04-03: P1-09 再推进。新增 wren-ai-service/src/core/legacy_ask_tool.py，把 AskService 里的 legacy NL2SQL/general/historical 主流程抽到独立 fallback tool；AskService 现在主要做入口、mode 分流、cache 状态更新回调与 deepagents orchestrator 调用。验证：py_compile(legacy_ask_tool/deepagents_orchestrator/core/__init__/ask) 通过；pytest(ask_skill_runner + ask_golden_regression + runtime_identity_bridge + test_config + wren_engine + runtime_scope_context) 45 passed。
[2026-04-03T02:03:44.129Z] 2026-04-03: P1-09 再下一步完成。新增 wren-ai-service/src/core/mixed_answer_composer.py，LegacyAskTool 统一通过 composer 组装 SKILL/GENERAL/TEXT_TO_SQL 返回载荷与 metadata；AskService 支持 mixed_answer_composer 注入，继续保持 mode router 角色。新增 ask_skill_runner 用例验证 composer 在 skill/text-to-sql 终态都被调用。验证：py_compile(core composer/tool/orchestrator + ask + tests) 通过；pytest(ask_skill_runner + ask_golden_regression + runtime_identity_bridge + test_config + wren_engine + runtime_scope_context) 47 passed。
[2026-04-03T02:12:56.179Z] 2026-04-03: P1-09 再推进。新增 wren-ai-service/src/core/tool_router.py；ToolRouter 现在在 deepagents 模式下先调用 DeepAgentsAskOrchestrator.run，再显式 fallback 到 LegacyAskTool；legacy 模式直接走 fallback_runner。DeepAgentsAskOrchestrator 新增 run()，在 skill-first 命中时直接经 composer 返回，否则交给 legacy fallback。AskService 进一步收敛成 mode router + status cache。验证：py_compile(core deepagents/tool_router/ask/test_ask_skill_runner) 通过；pytest(ask_skill_runner + ask_golden_regression + runtime_identity_bridge + test_config + wren_engine + runtime_scope_context) 49 passed。
[2026-04-03T03:23:36.231Z] 2026-04-03: 完成 wren-ui 基于 api_history.responsePayload.askDiagnostics 的 askShadowCompareStats GraphQL 聚合查询。新增 schema/resolver/tests，默认聚合 ASK+STREAM_ASK，支持 threadId/statusCode/time range/project scope 过滤；统计 total/withDiagnostics/enabled/executed/comparable/matched/mismatched/errorCount/byAskPath/byShadowErrorType。验证：apiHistoryResolver.test 4/4 pass，修改文件 LSP diagnostics 0 error。
[2026-04-03T03:35:59.343Z] 2026-04-03: 已将 askShadowCompareStats 接到 wren-ui 消费端。新增 api-management/history 顶部 Shadow compare rollout 摘要卡，支持基于当前 thread/status/apiType(ASK/STREAM_ASK) 过滤显示 total/coverage/executed/match/error/top ask paths/top shadow errors；非 ASK 类筛选时显示提示。同步补了 client gql 文档与手工维护的 generated/__types__，新增组件测试通过。全量 check-types 仍失败，但仅是既有基线问题（vega-lite 类型、graphql context、多个页面 SiderLayout children）。
[2026-04-03T03:39:26.745Z] 2026-04-03: 已完成 api history Details Drawer 的 askDiagnostics 结构化展示。新增 Ask diagnostics 区块，展示 traceId/askPath 以及 shadow compare 的 enabled/executed/comparable/matched 状态、primary/shadow path/type/result counts/error types/reason/shadow error。新增 DetailsDrawer.test.tsx，联合 shadow summary + resolver tests 共 7/7 通过，改动文件 LSP diagnostics 0 error。
[2026-04-03T03:43:07.175Z] 2026-04-03: 已完成 API history 列表页 diagnostics 摘要列。新增共享 askDiagnostics 解析工具 askDiagnostics.ts，并新增 AskDiagnosticsSummary 组件，在列表页按 ASK/STREAM_ASK 记录展示 askPath、matched/mismatched、shadowErrorType；非 ask 记录显示 '-'。DetailsDrawer 改为复用共享解析逻辑。验证：AskDiagnosticsSummary/DetailsDrawer/ShadowCompareSummary/apiHistoryResolver 共 4 suites 9 tests 全通过，相关文件 LSP diagnostics 0 error。
[2026-04-03T03:45:09.677Z] 2026-04-03: 已增强 API history 列表摘要，AskDiagnosticsSummary 现在会在 tag 下方展示 shadowCompare.reason 的截断预览，并用 Tooltip 暴露完整 reason，便于快速定位 mismatch 原因。验证：相关 4 suites 9 tests 通过，AskDiagnosticsSummary 文件与测试 LSP diagnostics 0 error。
[2026-04-03T04:01:03.297Z] 2026-04-03: 已将 askShadowCompareStats 从 resolver 内存扫描下沉到 apiHistoryRepository SQL 聚合。新增 repository 接口 getAskShadowCompareStats，按方言生成 sqlite(json_extract/json_type) 与 pg(jsonb #>/#>>) 聚合查询，并输出 summary/byAskPath/byShadowErrorType；resolver 改为仅做 scope/filter 校验与委托。新增 repository SQL generation tests + resolver/UI tests，共 5 suites 11 tests 通过；相关 repository/resolver 文件 LSP diagnostics 0 error。
[2026-04-03T04:08:05.416Z] 2026-04-03: 已为 askShadowCompareStats 增加 daily 趋势桶。repository 统计结果新增 trends[{date,total,executed,comparable,matched,mismatched,errorCount}]，schema/client/generated/types 已同步，ShadowCompareSummary 新增 Recent trend 展示最近 7 个日期桶。验证：apiHistoryRepository/resolver + apiManagement 组件共 5 suites 11 tests 通过，相关文件 LSP diagnostics 0 error。
[2026-04-03T04:13:27.394Z] 2026-04-03: 已为 API history 页面补时间范围筛选。新增 timeRange helper（buildApiHistoryDateFilter / presets / hasRange）与测试；history.tsx 顶部加入 RangePicker + Reset，并将 startDate/endDate 同步传给列表 query 与 askShadowCompareStats query，同时分页 current 改为受控，筛选变化会重置到第一页。验证：新增 timeRange.test + 既有 repository/resolver/ui tests 共 6 suites 14 tests 通过，相关文件 LSP diagnostics 0 error。
[2026-04-03T06:53:53.193Z] 2026-04-03: 在 P1-11 验证链路继续补了 misc/scripts/scan-runtime-identity.sh。新脚本同时扫描 wren-ui 主路径 getCurrentProject() 残留和 wren-ai-service/src/web 直接 .project_id/.projectId 访问残留，当前均仅剩 allowlisted bridge 用法。验证：bash -n + bash scan-runtime-identity.sh 通过；scan-current-project.sh 回归通过。
[2026-04-03T07:01:17.548Z] 2026-04-03: 回到 P1-09。已为 ToolRouter 增加 deepagents primary path 异常时的安全回退：若 deepagents orchestrator 抛异常，会自动 fallback 到 legacy ask，并在 metadata 写入 fallback_reason=deepagents_error / deepagents_error 文本；开启 ask_shadow_compare 时，skipped shadow_compare 的 reason 也会记录为 deepagents_error。新增 ask_skill_runner 回归用例覆盖 router fallback 与 AskService stats by_reason。验证：pytest(test_ask_skill_runner + ask_golden_regression + runtime_identity_bridge) 37 passed；py_compile 通过。
[2026-04-03T07:25:51.538Z] Started ultrawork parallel lanes on 2026-04-03: P1-09 deepagents ask hardening -> Jason (019d523b-5da7-7231-a8fd-271c03a681d1); P1-10 postgres+pgvector default cutover -> Curie (019d523b-1a2b-7350-a4aa-a706ebf1b6f0); P1-02 auth/session closure -> Nietzsche (019d523b-8dd6-7f41-a248-aff2f40e1f21). Main agent owns integration/light verification.
[2026-04-03T07:26:21.322Z] Started ultrawork-style parallel execution on 2026-04-03 for three independent Phase 1 lanes: P1-09 deepagents ask hardening, P1-10 PostgreSQL+pgvector default cutover, P1-02 auth/session closure. Ownership boundaries assigned to minimize conflicts; next leader step is integrate outputs and run lightweight verification.
[2026-04-03T07:27:23.021Z] Started $ultrawork parallel Phase 1 lanes on 2026-04-03. Curie= P1-09 deepagents ask hardening (agent 019d523b-1a2b-7350-a4aa-a706ebf1b6f0). Confucius= P1-10 PostgreSQL+pgvector default cutover (agent 019d523b-1cda-74b0-ac30-bffec30f8613). Halley= P1-02 auth/session/workspace closure (agent 019d523c-5c74-7e43-8a91-c7d0a9ed6f6b). Next: integrate results and run lightweight verification.
[2026-04-03T08:41:46.764Z] 2026-04-03: Closed minimal P1-05 dashboard/schedule runtime binding. Added ScheduleService (schedule_job upsert/deactivate for dashboard_refresh), wired DashboardResolver.setDashboardSchedule to sync job with runtime scope, added ScheduleWorker bootstrap in common.ts, disabled dashboard tracker polling in app bootstrap, and reused DashboardCacheBackgroundTracker as executor via new refreshDashboardById(). Targeted jest suites passed (5 suites / 61 tests). Full wren-ui check-types still blocked by pre-existing unrelated vega-lite and LayoutSkeleton children errors.
[2026-04-03T08:52:19.911Z] 2026-04-03: Continued P1-05 beyond minimal schedule sync. Added dashboard runtime binding migration (knowledge_base_id/kb_snapshot_id/deploy_hash/created_by), dashboard runtime resolver util, DashboardService.getCurrentDashboardForScope + syncDashboardRuntimeBinding, updated DashboardResolver to prefer scope-bound dashboard and deployment hash, and changed DashboardCacheBackgroundTracker to resolve project/deploy from dashboard binding via kb_snapshot. Targeted jest now passes 5 suites / 63 tests; file-level LSP diagnostics 0 errors on all touched files. Full check-types still blocked by unrelated repo-wide vega-lite/LayoutSkeleton issues.
[2026-04-03T09:28:56.218Z] 2026-04-03: Hardened dashboard scoped creation to avoid cross-KB rebinding. DashboardService.initDashboard now accepts optional binding, getCurrentDashboardForScope prefers scoped dashboard, reuses only unbound legacy dashboard, otherwise creates a new scoped dashboard. Added guard against rebinding a dashboard to another KB. ProjectResolver.createProjectFromDataSource now passes current runtime binding into initDashboard. Targeted jest passed: projectResolver/dashboardResolver/dashboardService/dashboardCacheBackgroundTracker = 4 suites / 67 tests.
[2026-04-03T09:47:10.958Z] 2026-04-03: P1-05 client收口继续推进。手工从 wren-ui/src/apollo/client/graphql/__types__.ts 的 Dashboard base type 去掉 legacy projectId 暴露；确认 dashboard.generated.ts / dashboard.ts / pages/home/dashboard.tsx 不再出现 dashboard-specific projectId。验证通过：dashboardResolver + pages/api/graphql targeted jest 2 suites / 12 tests passed，__types__.ts 与 dashboardResolver.ts LSP diagnostics 0 errors。
[2026-04-03T10:16:28.264Z] 2026-04-03: 继续推进技能控制面收口，新增 wren-ui/src/pages/knowledge/skills.test.tsx，覆盖 (1) runtime scope 缺失时 GraphQL query skip；(2) skill definitions/bindings 页面把 workspace/KB/snapshot/connector 标签正确映射到 UI 表达。验证：yarn test --runInBand src/pages/knowledge/skills.test.tsx 通过；文件 LSP diagnostics 0 errors。
[2026-04-03T10:26:46.133Z] 2026-04-03: 继续推进 P1-06 connector/control-plane。新增 REST endpoints pages/api/v1/connectors/index.ts 与 [id].ts，支持 active KB scope 下的 list/create/get/update/delete，并返回 redacted connector metadata（hasSecret 而非 secret 明文）。同时扩展 ApiType 增加 GET/CREATE/UPDATE/DELETE_CONNECTOR，新增 connectors_api.test.ts 覆盖列表、创建（含 secret）、跨 KB 拒绝、更新/删除。验证：connector+secret+skills API targeted jest 4 suites / 18 tests passed；新文件 LSP diagnostics 0 errors。
[2026-04-03T11:10:32.742Z] 2026-04-03: 继续把 P1-06 connector API 接到 P1-07 技能页面。skills.tsx 现在在有 runtime scope 时会请求 runtime-scoped /api/v1/connectors，并把 binding 表单中的 connectorId 从手填 Input 改成 Select；同时导出 buildSkillConnectorOptions/buildSkillConnectorsApiUrl 纯函数供测试。验证：yarn test --runInBand src/pages/knowledge/skills.test.tsx src/pages/api/tests/connectors_api.test.ts -> 2 suites / 7 tests passed；skills.tsx 与 skills.test.tsx LSP diagnostics 0 errors。
[2026-04-03T11:26:31.575Z] 2026-04-03: 继续推进 P1-06 前端管理面。新增 knowledge/connectors 页面与 sidebar 接入：支持 runtime-scoped connectors 列表/创建/编辑/删除，使用 REST /api/v1/connectors 与 /api/v1/connectors/[id]；新增 Path.KnowledgeConnectors 和 MENU_KEY.CONNECTORS，并把 Knowledge 侧边栏加入 Connectors 入口。验证：yarn test --runInBand src/pages/knowledge/connectors.test.tsx src/pages/knowledge/skills.test.tsx src/pages/api/tests/connectors_api.test.ts -> 3 suites / 9 tests passed；connectors.tsx/connectors.test.tsx/sidebar Knowledge.tsx LSP diagnostics 0 errors。
[2026-04-03T12:00:50.717Z] 2026-04-03: 继续打通 connector/skill 管理链路。skills.tsx 的 Skill bindings 卡片新增 runtime-scoped 'Manage connectors' 链接，并在 connectors 为空时显示引导文案到 /knowledge/connectors；skills.test.tsx 新增对应断言，验证导航 href 保留 knowledgeBase scope。验证：yarn test --runInBand src/pages/knowledge/skills.test.tsx src/pages/knowledge/connectors.test.tsx -> 2 suites / 5 tests passed；skills.tsx 与 skills.test.tsx LSP diagnostics 0 errors。
[2026-04-03T12:04:35.327Z] 2026-04-03: 修复 connector 管理页一个关键行为坑。connectors.tsx 现已抽出 buildConnectorSubmitPayload，并在编辑 connector 时若 secret JSON 留空则保持现有 secret 不变，不再误删 secret；同时新增 CONNECTOR_SECRET_EDIT_HINT 提示文案。connectors.test.tsx 新增对应纯函数测试，覆盖 create/edit secret 提交语义。验证：yarn test --runInBand src/pages/knowledge/connectors.test.tsx src/pages/knowledge/skills.test.tsx src/pages/api/tests/connectors_api.test.ts -> 3 suites / 10 tests passed；connectors.tsx/connectors.test.tsx LSP diagnostics 0 errors。
[2026-04-03T12:54:59.757Z] 2026-04-03: Continued P1-10 cleanup. Updated wren-ai-service/src/providers/__init__.py examples from qdrant to pgvector, removed obsolete version key from docker/docker-compose.yaml, migrated docker/docker-compose-dev.yaml from qdrant to pgvector/postgres, and refreshed docker/README.md service/startup wording. Verified both compose files with `docker compose ... config`; provider tests passed (8 passed).
[2026-04-03T13:00:30.911Z] 2026-04-03: Continued P1-10 verification hardening. Added shared require_pgvector_runtime() helper in wren-ai-service/tests/pytest/conftest.py and wired service/indexing pgvector-backed tests to skip cleanly when pgvector optional deps are absent instead of failing during container/store construction. Verified: service sql_pairs+instructions => 12 skipped, indexing sql_pairs+instructions => 6 skipped, providers+ask => 8 passed 1 skipped.
[2026-04-03T13:54:39.730Z] 2026-04-03: Continued pgvector runtime verification. Improved src/providers/document_store/pgvector.py to surface incompatible-runtime errors (not just missing dependency errors), added provider regression coverage, and refined require_pgvector_runtime() skip reasons. Verified current local env imports fail due `cannot import name 'convert' from haystack.utils.filters`. Temp venv experiments also reproduced the same import error for (haystack-ai 2.7.0 + pgvector-haystack 0.4.0) and (haystack-ai 2.24.0 + pgvector-haystack 0.5.1), so real pgvector smoke remains blocked by upstream package compatibility rather than repo code.
[2026-04-03T15:44:05.220Z] 2026-04-03 P1-10 pgvector/sql_pairs 收口：新增 deterministic test embedder（tests/pytest/conftest.py），sql_pairs 空 question 校验（src/pipelines/indexing/sql_pairs.py），并在 pgvector provider get_store(recreate_table=True) 时强制 eager init，修复 lazy recreate 导致的 fixture reset/计数异常。已验证 services/pipelines/providers 相关套件通过。
## PRIORITY
Resume from commit ea4f7ae0 on main. Current focus: WrenAI V1 rebuild; latest completed slice is workspace-scoped skills + runtime-aware ask flows. If Codex LiteLLM base_url/key changes, start a new Codex session in this repo instead of reusing the old Responses conversation.
